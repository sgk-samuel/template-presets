/** @type {import('prettier').Config} */
const config = {
  semi:false,
  trailingComma:'es5',
  singleQuote:true,
  tabWidth:2,
  overrides:[
    {
      files:[],
      options:{
        
      }
    }
  ]
}

export default config
